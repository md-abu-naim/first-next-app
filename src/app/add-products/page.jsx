import React from 'react';
import Add from './Add';

const AddProducts = () => {
    return (
        <div>
            add products
            <Add />
        </div>
    );
};

export default AddProducts;